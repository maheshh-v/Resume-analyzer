# Handoff — `feat/eval-observability-connectors-api`

Four phases, each committed in logical units and pushed for incremental review. The full test
suite is green after every phase. Nothing in `core`/citation-guardrail (`app/pipeline/citation.py`)
or the Evidence Ledger hash-chain (`app/ledger.py`) was modified, and no scoring, ranking, or
auto-verdict was introduced.

> Path note: the prompt referenced `core/evidence_validator.py` and `app/evidence/connectors/`,
> which don't exist in this repo. The real citation guardrail is `app/pipeline/citation.py` and the
> connector pattern lives in `app/pipeline/evidence/`. Those were treated as the sacred / pattern
> files respectively.

## What shipped

### Phase 1 — Eval harness + public `/benchmarks` page
- `evals/` at repo root: `datasets/golden_v1.jsonl` (15 hand-labelled cases — 5 real, 5 planted-lie,
  5 edge), generated by `datasets/build_golden.py` which **asserts every quoted span is a literal
  substring** so a case can never ship uncitable. `datasets/MAKE_MORE.md` explains adding cases.
- Three runners over the **real pipeline stages** (offline via recorded LLM fixtures — no paid calls):
  `run_claim_extraction` (P/R/F1), `run_citation_validity` (guardrail span check, target 100%),
  `run_verdict_accuracy` (verdict match + **fabrication-safety**, target 100%).
- `evals/report.py` → `evals/results/{latest.json, latest.md, <date>.json}`. `evals/README.md`.
- Backend `GET /api/v1/benchmarks/latest` (public, no-auth, DB-free).
- Frontend public page `/benchmarks` (added to middleware public prefixes).
- `Makefile` (`eval`, `eval-report`, `eval-build`, `eval-test`, `test-api`, `test`).

### Phase 2 — Langfuse observability + cost tracking
- Langfuse was already partially wired; this **extended** it. Every LLM call now tags its span with
  `candidate_id`, `job_id`, `pipeline_stage`, `model`, `retry_count`.
- New `llm_call_log` table (metadata only — **no prompt/response bodies**). `app/observability/`:
  a contextvar carrying ambient candidate/job ids, and a best-effort cost sink on its own session.
- `GET /api/v1/candidates/{id}/llm-costs` — ownership-gated per-stage rollup + totals.
- Recruiter candidate-detail page: collapsible **AI pipeline trace** panel.

### Phase 3 — Evidence connectors
- `app/pipeline/evidence/citation_guard.py` — the non-negotiable **URL-200 + literal-substring**
  guardrail (delegates the substring test to the sacred `citation.snippet_is_literal_substring`).
- `semantic_scholar.py`, `google_patents.py` (aggressive per-inventor cache), `package_ownership.py`
  (npm/PyPI). Async, injectable client, **degrade to unverified** on any failure, per-connector config
  flag (off by default), wired into orchestrate + sealed with a `connector_evidence` ledger event.

### Phase 4 — White-label public API + Stripe metering (scaffold)
- `api_keys` + `public_reports` tables. Key-auth (`Authorization: Bearer` / `X-API-Key`), hashed at
  rest, per-key rate limit (in-memory, 10/min) + monthly quota.
- `POST /api/v1/public/verify` (async; resume as base64 PDF or text; one metered event) and
  `GET /api/v1/public/reports/{id}` (key-scoped; JSON + signed PDF URL).
- Branded PDF via PyMuPDF (org name + logo slot), stored via Supabase Storage (local disk fallback).
- `app/billing/meter.py` — Stripe metered-usage **scaffold** (no charges, no card collection, lazy
  import, no-op unless a key has `stripe_customer_id` and `STRIPE_API_KEY` is set).
- Admin CLI: `python -m app.cli create_api_key --org "Acme Staffing" --quota 500`.

## New env vars

All optional; sensible defaults keep everything working with zero config.

| Var | Default | Purpose |
|---|---|---|
| `BENCHMARKS_RESULTS_DIR` | repo `evals/results` | Where the benchmarks endpoint reads results |
| `LANGFUSE_PUBLIC_KEY` / `LANGFUSE_SECRET_KEY` / `LANGFUSE_HOST` | unset → tracing no-op | LLM tracing |
| `ENABLE_SEMANTIC_SCHOLAR` / `ENABLE_GOOGLE_PATENTS` / `ENABLE_PACKAGE_OWNERSHIP` | `false` | Enable each connector |
| `PUBLIC_API_RATE_LIMIT_PER_MIN` | `10` | Per-key rate limit |
| `STRIPE_API_KEY` | unset → billing no-op | Stripe metered billing |
| `STRIPE_METER_EVENT_NAME` | `recruitx_verification` | Stripe meter event name |
| `SUPABASE_STORAGE_BUCKET` | `resumes` | Private bucket for candidate resumes |
| `SUPABASE_REPORTS_BUCKET` | `recruitx-reports` | Private bucket for white-label branded PDFs |
| `SUPABASE_SERVICE_ROLE_KEY` | unset | Admin key, **server-side only** — put a real value in `.env` only, never `.env.example`/logs/commits |
| `USE_LOCAL_STORAGE` | `false` | Force local-disk storage (dev/testing without Supabase creds) |

## New DB migrations

From `apps/api`, with `DATABASE_URL` pointed at the target DB:

```bash
alembic upgrade head
```

- `0003_llm_call_log` — per-call cost/latency telemetry (metadata only).
- `0004_public_api` — `api_keys` + `public_reports`.

## Run the eval harness locally

```bash
make eval          # run the 3 runners, print JSON
make eval-report   # aggregate into evals/results/ (feeds the /benchmarks page + endpoint)
make eval-test     # the harness's own tests
make eval-build    # regenerate golden_v1.jsonl + fixtures
```

Current numbers (offline `recorded-fixture` provider): extraction F1 **98.4%**, citation validity
**100%**, verdict match **100%**, fabrication safety **100%** (0/6 planted lies falsely verified).

## Create an API key

```bash
cd apps/api
alembic upgrade head                                   # once, so api_keys exists
python -m app.cli create_api_key --org "Acme Staffing" --quota 500
# prints the raw key ONCE — only its SHA-256 is stored
```

## Known gaps / TODOs

- **Benchmarks are fixture-based (offline).** The numbers measure the pipeline's own processing
  (guardrail, normalization, consistency, verdicts) against recorded model output. A live-model pass
  is a paid, gated step — re-record `evals/fixtures/llm/*.json` against the model and set `provider`.
- **Public `/verify` runs a lighter, un-ledgered pipeline** — no GitHub/connector passes and no
  Evidence Ledger. Wiring it through the full ledgered Candidate flow is a follow-up.
- **package_ownership auto-invocation** uses `github_login` as an npm-maintainer handle (heuristic);
  explicit PyPI/package ownership needs a claimed-packages field on the candidate (not modelled yet).
- **Supabase Storage for branded PDFs is wired** (private `recruitx-reports` bucket, 7-day signed
  URLs). Needs the bucket created + the service key in `.env`; without them (or with
  `USE_LOCAL_STORAGE=true`) it falls back to local disk and returns `pdf_url: null`. See the
  "Supabase Storage" section above.
- **Stripe is scaffold-only:** onboarding a customer (Stripe customer + meter) is manual; no events
  fire without `STRIPE_API_KEY` + a key's `stripe_customer_id`. No card collection anywhere.
- **Rate limiter is in-memory, single-process** — a multi-instance deploy should move it to Redis.
- **Legacy-redirect layer is temporary.** Old unprefixed recruiter paths 308-redirect to `/api/v1`
  and log a warning on each hit (`app/main.py:LegacyPathRedirectMiddleware`). Remove it once no
  legacy traffic shows up in logs.

## API paths (all now under `/api/v1`)

Every product route is versioned under `/api/v1`. `/health` and `/` stay unprefixed (infra health
checks). Old bare paths **308-redirect** to their `/api/v1` equivalents (308 preserves method +
body) — already-sent interview invite links are *frontend* routes (`/interview/{token}` on the web
app) and are unaffected; only the API calls that page makes moved.

| Old (redirected) | New |
|---|---|
| `/jobs`, `/jobs/{id}`, `/jobs/{id}/requirements`, `/jobs/{id}/candidates` | `/api/v1/jobs...` |
| `/candidates/{id}`, `/candidates/{id}/resume` | `/api/v1/candidates...` |
| `/candidates/{id}/interviews`, `/candidates/{id}/interviews/{iid}/transcript` | `/api/v1/candidates...` |
| `/candidates/{id}/report`, `/candidates/{id}/decision` | `/api/v1/candidates...` |
| `/candidates/{id}/ledger`, `/candidates/{id}/ledger/verify` | `/api/v1/candidates...` |
| `/interview/{token}`, `/interview/{token}/questions/{qid}/answer` | `/api/v1/interview...` |
| (already versioned) benchmarks / `llm-costs` / public | `/api/v1/...` (unchanged) |
| `/health`, `/` | unchanged (never redirected) |

The frontend prepends `/api/v1` once, in `apps/web/src/lib/api-client.ts` (`request()`); non-API
fetches like `/health` bypass it.

## Supabase Storage for branded PDFs (`recruitx-reports`)

The white-label `/verify` flow stores each branded PDF in a **private** Supabase Storage bucket and
returns a **7-day signed URL** (`app/storage/report_storage.py`). Local disk is the fallback for dev.

**Manual step (Mihir):** in the Supabase dashboard → Storage → **New bucket**, create
`recruitx-reports` with **Public unchecked** (private). Reports must never be world-readable.

**Configure** (in `apps/api/.env`, real values there **only** — never `.env.example`):
```
SUPABASE_URL=https://<project-ref>.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<paste the service_role key here — server-side only>
SUPABASE_REPORTS_BUCKET=recruitx-reports
USE_LOCAL_STORAGE=false          # set true to force local disk for local testing
```

**Service-key safety (enforced):** the key is read from settings only — it is never logged, never
returned in an API response, and never committed (`.env` is gitignored — verified via
`git check-ignore apps/api/.env`). Only its use as an `Authorization: Bearer` header (server→Supabase)
touches it.

**Rotating the service key:** Supabase dashboard → Settings → API → roll the `service_role` key,
then paste the new value into `apps/api/.env` (`SUPABASE_SERVICE_ROLE_KEY`) and restart the API. No
code change needed. Rotate immediately if a key was ever shared in plaintext (e.g. chat/email).

## Test count: before vs after

| | Before | After |
|---|---|---|
| Backend (`apps/api`, `pytest --collect-only`) | 88 | 147 |
| Eval harness (`evals/tests`) | 0 | 26 |
| **Total** | **88** | **173** |

(+6 legacy-redirect tests, +8 storage tests since the first four phases.)

Full suite green after each phase (`pytest -x`).

## Screenshots

- `/benchmarks` page — **verified rendering** via the in-app browser: SSR returns HTTP 200 with all
  four metric cards, methodology, and the dataset link, confirmed by `read_page` (DOM dump captured
  in the session log). PNG capture was blocked because the browser pane in this session is anchored
  to a different repo directory (`D:\Mahesh\Portfolio`), so the screenshot tool timed out. **PNG
  pending** — reproduce with `npm --prefix apps/web run dev` + the API running, then open
  `http://localhost:3000/benchmarks`.
- AI pipeline trace panel — behind Supabase recruiter auth, so not captured live; verified via
  `tsc --noEmit` (clean) and the endpoint's tests. **Screenshot pending** a logged-in session.
